flask
spotdl
yt-dlp
librosa
matplotlib
numpy
soundfile
# Tier 2 cookie recovery only (visible browser login). Needs a one-time
# `playwright install firefox`; the premium gate skips tier 2 when unattended.
playwright
